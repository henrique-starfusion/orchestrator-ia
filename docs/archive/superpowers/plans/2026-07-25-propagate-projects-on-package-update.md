# Propagate Projects on Package Update — Plan

**Goal:** Ao `orchestrator update` no pacote, atualizar automaticamente projetos registrados.

**Done in 0.4.20:** registry helpers em `Orchestrator.Common.ps1`, `Propagate-OrchestratorUpdate.ps1`, hooks em `Install-Orchestrator.ps1`, flags JS, `Test-ProjectPropagate.ps1`.
