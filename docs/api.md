# API overview

Status: **implementado** (CLI + MCP) · REST/Docker: **planejado**

| Superfície | Entrada |
|---|---|
| CLI runtime | `orchestrator run` / `task *` |
| MCP stdio/HTTP | `orchestrator mcp serve` |
| Cursor | `.cursor/mcp.json` → tools acima |

Não há API REST pública no MVP 0.3.0.
