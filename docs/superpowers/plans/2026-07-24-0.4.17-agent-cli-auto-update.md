# 0.4.17 — Auto-update de CLIs de agentes no install/update — Plano Refinado (execute_review_repair)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans para executar este plano tarefa a tarefa. Passos usam checkboxes (`- [ ]`).

**Goal:** Garantir que `orchestrator install|update` atualize por padrão os CLIs dos agentes já existentes no PC (opt-out `-SkipAgentUpdates`), com ordem de pipeline unificada e testes/docs/versionamento fechados.

**Architecture:** Iteração anterior já implementou ~95% do escopo (Update-Agents.ps1 reescrito, maps npm/choco/scoop, flags no Install-Orchestrator.ps1 e bin/orchestrator.js, docs, migration, VERSION 0.4.17 em todos os arquivos). Este plano é de **verificação + reparo**: fecha 2 gaps reais encontrados por inspeção do código e delega execução de testes ao runtime (sessão autônoma não pode executar npm/pytest — ver memória `autonomous-session-exec-denied`).

**Tech Stack:** PowerShell 5.1 (scripts/), Node (bin/orchestrator.js), suite de testes PowerShell (tests/), pytest via `npm run test:runtime`.

## Global Constraints

- NÃO instalar agentes ausentes — só atualizar `status=available` em `detected.json` (spec §5).
- Falhas de update de CLI = avisos; nunca abortam install/update (exit 0 de Update-Agents.ps1 já garante).
- `-UpdateAgents` permanece aceito por compatibilidade (não remover).
- Agentes IDE (cursor/kiro) sempre pulados (`Test-IsIdeAgent`).
- Sessão autônoma: proibido executar git/npm/pytest/node/orchestrator — tarefas de execução ficam com o tester `runtime` ou sessão interativa.
- Versão alvo: 0.4.17 (já aplicada em VERSION, package.json, runtime/pyproject.toml, `__init__.py`, template VERSION).

## Estado verificado (não refazer)

| Item da spec | Estado | Evidência |
|---|---|---|
| Update-Agents.ps1 expandido (nativo → npm → choco → scoop, DryRun, report JSON, skip IDE, default sem flag) | ✅ feito | `scripts/Update-Agents.ps1:1-217` |
| Maps `Get-AgentNpmPackageMap` / `Get-AgentChocolateyPackageMap` (opencode) / `Get-AgentScoopPackageMap` | ✅ feito | `scripts/Orchestrator.Common.ps1:807-834` |
| `-SkipAgentUpdates` + default ON no comando `update` (ordem Detect → Update → re-Detect → Validate) | ✅ feito | `scripts/Install-Orchestrator.ps1:345-359` |
| `-SkipAgentUpdates` + default ON no comando `install` | ⚠️ feito, mas ordem errada | `scripts/Install-Orchestrator.ps1:707-722` roda DEPOIS de Validate (linha 643) — Task 1 |
| CLI `--skip-agent-updates` → `-SkipAgentUpdates` | ✅ feito | `bin/orchestrator.js:227-228` |
| Docs README / cli-reference / installer-architecture / orquestrador / troubleshooting | ✅ feito | `README.md:267-301`, `docs/cli-reference.md:354-371`, `docs/installer-architecture.md:115-116`, `docs/orquestrador.md:159`, `docs/troubleshooting.md:361-365` |
| `tests/Test-AgentUpdates.ps1` (dry-run default + skip path) | ✅ feito | `tests/Test-AgentUpdates.ps1:17-43` |
| VERSION/CHANGELOG/migration/template/pyproject/`__init__`/package.json 0.4.17 | ✅ feito | `CHANGELOG.md:5-21`, `package/migrations/0.4.16-to-0.4.17.ps1` |
| Não instalar agentes ausentes | ✅ feito | Update-Agents só itera `status -eq 'available'` (linha 83) e pula `skipped_missing` |

---

### Task 1: Unificar ordem do pipeline `install` (Update-Agents antes de Validate)

Spec §1 exige ordem unificada `Detect-Agents → Update-Agents → Detect-Agents (re-detect) → Validate…`. O comando `update` cumpre; o comando `install` roda Update-Agents na linha 707, DEPOIS de `Validate-Orchestrator.ps1` (linha 643).

**Files:**
- Modify: `scripts/Install-Orchestrator.ps1:599` (inserir bloco após Detect-Agents inicial)
- Modify: `scripts/Install-Orchestrator.ps1:707-722` (remover bloco tardio)
- Modify: `docs/installer-architecture.md:108-117` (reordenar lista numerada do pipeline install)

- [x] **Step 1: Mover bloco no Install-Orchestrator.ps1** — feito. Bloco do caminho `install` movido de 707→ depois do `Detect-Agents` inicial (`Install-Orchestrator.ps1:600-614`). O caminho `update` também foi unificado: bloco movido de depois de `Configure-Mcps` para logo após o `Detect-Agents` de refresh (`Install-Orchestrator.ps1:300-311`), antes de `Generate-Adapters` — assim adapters e validação enxergam versões pós-update nos dois comandos.

Remover as linhas 707-722 (bloco `# 0.4.17: atualizar CLIs...` até o `else { Write-Host '[INFO] Update-Agents ignorado...' }` do caminho install) e inserir o mesmo bloco imediatamente após a linha 599 (`Invoke-ChildScript -Name 'Detect-Agents.ps1' ...`), antes de `$adapterArgs`:

```powershell
    Invoke-ChildScript -Name 'Detect-Agents.ps1' -Arguments @{ ProjectPath = $projectRoot } | Out-Null

    # 0.4.17: atualizar CLIs de agentes existentes por padrao (opt-out: -SkipAgentUpdates)
    if (-not $SkipAgentUpdates) {
        Write-Host '[ETAPA] Update-Agents (CLIs existentes no PATH)'
        $updateArgs = @{
            ProjectPath  = $projectRoot
            UpdateAgents = $true
        }
        if ($Force) { $updateArgs.Force = $true }
        if ($DryRun) { $updateArgs.DryRun = $true }
        Invoke-ChildScript -Name 'Update-Agents.ps1' -Arguments $updateArgs | Out-Null
        Invoke-ChildScript -Name 'Detect-Agents.ps1' -Arguments @{ ProjectPath = $projectRoot } | Out-Null
    }
    else {
        Write-Host '[INFO] Update-Agents ignorado (-SkipAgentUpdates).'
    }

    $adapterArgs = @{
```

Resultado: install = Detect → Update-Agents → re-Detect → Generate-Adapters → … → Validate (adapters e validação enxergam versões pós-update).

- [x] **Step 2: Reordenar lista do pipeline em `docs/installer-architecture.md`** — feito (`installer-architecture.md:108-120`, renumerado 9→21). Também atualizados `docs/orquestrador.md:184-193,198-204` (pipelines install/update), `README.md:297-301` (ordem Detect→Update→Detect→Adapters) e `docs/cli-reference.md:369` (linha de ordem unificada).

Na lista numerada (linhas 100-121), mover as etapas `Update-Agents.ps1 (padrão ON; opt-out -SkipAgentUpdates)` e `Detect-Agents.ps1 (re-detect após update de CLIs)` para logo depois do primeiro `Detect-Agents.ps1` (atual passo 9) e renumerar:

```
9. Detect-Agents.ps1
10. Update-Agents.ps1              (padrão ON; opt-out -SkipAgentUpdates)
11. Detect-Agents.ps1              (re-detect após update de CLIs)
12. Generate-Adapters.ps1
13. Install-Tools.ps1              (salvo -SkipTools)
14. Configure-Mcps.ps1             (se -ConfigureMcps)
15. Validate-Orchestrator.ps1
16. Validate-Hooks.ps1
17. Legacy cleanup (pos): remove safe → validate
18. Probe-Agents.ps1               (skip por padrão; -RunSmokeTest ativa)
19. Configure-CursorMcp.ps1        (projeto + ~/.cursor/mcp.json; pular com -SkipCursor)
20. Write-InstallationReport.ps1   (inclui seção Legacy cleanup)
21. Remove-InstallationLock        (finally)
```

- [ ] **Step 3: Verificação estática (permitida em sessão autônoma)**

Reler `scripts/Install-Orchestrator.ps1` e confirmar: (a) bloco aparece exatamente 1 vez no caminho install; (b) nenhum resíduo nas antigas linhas 707-722; (c) `$SkipAgentUpdates`, `$Force`, `$DryRun` estão no escopo (são params do script — estão).

### Task 2: Documentar default no help do CLI (`bin/orchestrator.js`)

Spec §3: "Documentar que update de agentes é padrão". O mapeamento `--skip-agent-updates` existe (linha 228), mas `printHelp()` não menciona.

**Files:**
- Modify: `bin/orchestrator.js:19-47` (função `printHelp`)

- [ ] **Step 1: Acrescentar linha ao help**

Dentro do template string de `printHelp()`, após a linha `orchestrator legacy scan|cleanup|status|restore`, inserir:

```
  (install/update atualizam CLIs de agentes por padrão; opt-out: --skip-agent-updates)
```

- [ ] **Step 2: Verificação estática**

Reler `printHelp()` e confirmar template string válido (sem crase extra) — sem executar node.

### Task 3: Testes (delegado ao tester `runtime`)

Sessão autônoma não executa npm/pytest. O passo `run_tests` do plano do orquestrador (role tester=runtime) deve rodar:

- [ ] **Step 1:** `npm test` — inclui `tests/Test-AgentUpdates.ps1` (3 asserts: dry-run com flag, dry-run sem flag, `verify -SkipAgentUpdates`) e o restante da suite do installer.
- [ ] **Step 2:** `npm run test:runtime` — regressão pytest (nenhuma mudança Python neste release; deve passar limpo).
- [ ] **Step 3:** Se `Test-AgentUpdates` falhar após o reorder da Task 1, a causa provável é o caminho `verify` (que não roda o bloco — esperado, bloco fica só em install/update). Registrar em `.wolf/buglog.json` conforme regra do projeto.

### Task 4: Validação contra critérios de aceite (role validator)

- [ ] **AC-001** — `orchestrator update` sem flags chama Update-Agents: evidência `scripts/Install-Orchestrator.ps1:345-357` + `docs/cli-reference.md:354-369`.
- [ ] **AC-002** — `-SkipAgentUpdates` pula: branches `else` nos dois comandos + `tests/Test-AgentUpdates.ps1:34-43`.
- [ ] **AC-003** — kimi/claude/codex usam subcomando nativo; opencode via choco/npm conforme `installation_method`: `scripts/Update-Agents.ps1:45-51,127-165` + `Orchestrator.Common.ps1:807-834`.
- [ ] **AC-004** — re-detect após update: chamadas `Detect-Agents.ps1` imediatamente após `Update-Agents.ps1` nos dois comandos (após Task 1, install também antes de Validate).
- [ ] **AC-005** — Test-AgentUpdates + docs/version ok; falhas de update não quebram exit code (Update-Agents.ps1 sempre `exit 0`; resultado de `Invoke-ChildScript` descartado com `| Out-Null`).

### Fora de escopo (spec §5)

- Instalar agentes ausentes (`InstallMissingAgents`) — NÃO implementar.
- Update de agentes IDE (cursor/kiro) — mantidos como `skipped_ide`.

### Pendências pós-plano (sessão interativa)

- Commit + push develop (mensagem: `release: 0.4.17 — auto-update de CLIs de agentes no install/update`).
- `orchestrator update -Force` no(s) projeto(s) consumidores + reload MCP.
