# Memory and learning

SQLite operacional:

- `memories` (episódios)
- `agent_performance`
- `strategy_performance`

Busca: tokens simples / ranking (sem fine-tuning nesta fase).

Export markdown opcional em `.orchestrator/memory/episodes/`.
