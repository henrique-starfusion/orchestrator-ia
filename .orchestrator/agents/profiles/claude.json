{
  "id": "claude",
  "kind": "cli",
  "verified": true,
  "invoke": {
    "subcommand": [],
    "prompt_via": "arg",
    "prompt_flag": "-p",
    "sandbox_flags": ["--permission-mode", "bypassPermissions"]
  },
  "output": {
    "format": "text",
    "json_flags": ["--output-format", "json"]
  },
  "exit_codes": { "success": 0 },
  "timeout_default_s": 2400,
  "notes": "Aliases de modelo (haiku/sonnet/opus/fable) e model_flag vem de config/models.json. -p = modo print nao-interativo. 0.4.28: bypassPermissions (era acceptEdits) — acceptEdits auto-aceita EDICAO mas ainda pede aprovacao de Bash; sem terminal o agente spawnado ficava pendurado ate o timeout de 2400s ao tentar rodar teste/git/npm. Escopo segue contido: cwd travado no projeto (assert_within_project) e ORCHESTRATOR_CHILD_AGENT bloqueia delegacao aninhada. 0.4.29: prompt vai por stdin automaticamente quando argv passaria de 30000 chars (Windows corta em 32767 — WinError 206); invoke.prompt_via='stdin' forca o modo."
}
