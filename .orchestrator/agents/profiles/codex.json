{
  "id": "codex",
  "kind": "cli",
  "verified": true,
  "invoke": {
    "subcommand": ["exec"],
    "prompt_via": "arg",
    "prompt_flag": null,
    "sandbox_flags": ["--sandbox", "workspace-write", "--skip-git-repo-check"]
  },
  "output": {
    "format": "text",
    "json_flags": ["--json"]
  },
  "exit_codes": { "success": 0 },
  "timeout_default_s": 2400,
  "notes": "codex exec = modo nao-interativo; prompt posicional. model_flag -m vem de config/models.json. sandbox_flags usa workspace-write como base documentada; no Windows (os.name==nt) o adapter substitui automaticamente por danger-full-access para evitar CreateProcessAsUserW erro 740 (requer elevação)."
}
