# Run Tests

Execute project tests and capture outcomes for validation.

## When to use

- After implementation steps
- Before accepting a task as complete

## Outputs

- Test report in `runtime/reports/`
- Failures linked in `memory/failures/` when recurring
