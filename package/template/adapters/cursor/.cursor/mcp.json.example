{
  "mcpServers": {
    "multiagent-orchestrator": {
      "command": "cmd",
      "args": [
        "/c",
        "orchestrator",
        "mcp",
        "serve",
        "--transport",
        "stdio",
        "--project",
        "${workspaceFolder}"
      ],
      "enabled": true
    }
  }
}
