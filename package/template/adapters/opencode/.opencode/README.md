# OpenCode Adapter

This directory is a thin adapter. The canonical source is:

```text
../.orchestrator/
```

OpenCode should load shared orchestrator configuration, skills, memory, hooks, and runtime paths from `.orchestrator/`. Keep only OpenCode-specific entry points here.

---

Copyright StarFusion / Henrique Rodrigues 2026
